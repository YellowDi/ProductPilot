# ProductPilot 项目说明

## 语言与提交

- 日常沟通优先使用中文。
- git commit message 必须使用英文。

## 产品目标

ProductPilot 是一个面向拼多多商家后台的本地桌面自动化工具。

核心目标：

- 从准备好的商品资料中读取标题、图片、价格、库存、SKU、物流/售后等信息。
- 通过控制 Chrome/Chromium 浏览器自动填写拼多多商家后台页面。
- 使用 OCR 辅助识别页面状态、异常弹窗、错误提示和图片文字。
- 批量为一个或多个店铺创建商品草稿，正式上架始终由人工操作。

明确不做：

- 不接入拼多多开放平台 API。该接口需要申请和付费，本项目不依赖它。
- 不绕过验证码、登录风控、账号安全验证或平台反自动化机制。
- 不用 OCR 作为主流程点击依据。主流程优先使用 DOM selector、文本定位、控件状态。
- 不追求第一版全类目通用。先选定一个类目打通稳定闭环。
- 不自动执行正式上架。程序只允许创建或更新草稿，`提交并上架` 永远由人工操作。

## 技术栈决策

默认技术栈：

- Python 作为自动化核心语言。
- Playwright 控制 Chrome/Chromium 浏览器。
- 独立浏览器 Profile 保存登录态。
- PaddleOCR 作为本地 OCR 优先方案，后续可插拔云 OCR。
- SQLite 保存任务队列、商品状态、日志索引和恢复点。
- Pydantic 用于商品资料和配置校验。
- PySide6 作为跨平台桌面 UI，优先支持 macOS 和 Windows。
- pytest 做核心逻辑测试。
- PyInstaller 或 Nuitka 做桌面应用打包。

平台要求：

- 需要考虑 Windows 版，不默认采用 SwiftUI。
- 所有路径处理必须使用 `pathlib.Path`，避免写死 macOS 路径格式。
- Chrome 路径、用户数据目录、图片目录和导出目录必须可配置。

## 架构边界

建议模块边界：

- `product_pilot.domain`：商品资料、SKU、任务状态、流程步骤等纯业务模型。
- `product_pilot.automation`：Playwright 浏览器控制、页面定位、表单填写、截图和 trace。
- `product_pilot.ocr`：OCR 引擎接口、PaddleOCR 实现、识别结果标准化。
- `product_pilot.storage`：SQLite 持久化、任务队列、执行日志。
- `product_pilot.desktop`：PySide6 桌面 UI。
- `product_pilot.cli`：开发期命令行入口，用于校验资料和跑自动化 spike。

规则：

- UI 只做展示和控制，不承载核心业务逻辑。
- 自动化步骤必须是显式状态机，不能写成一个不可恢复的长脚本。
- 每个商品执行失败时必须保留当前步骤、错误原因、截图路径，条件允许时保留 Playwright trace。
- 删除文件、重置仓库、提交、推送等操作必须先得到用户明确要求。

## 多店铺批量草稿目标

当前目标是支持多个拼多多商家后台浏览器同时创建同一批商品草稿。

已确定的产品边界：

- 登录管理只管理登录态，不自动输入账号密码、短信验证码、扫码、风控或人机验证。
- 每个店铺使用独立 Chrome Profile 保存登录态，不共享 cookie 或浏览器数据。
- 桌面 UI 不暴露开发级运行配置；普通用户通过 `添加店铺浏览器` 添加账号。
- 用户每点击一次 `添加店铺浏览器`，程序创建一个独立 Profile，并打开一个新的商家后台浏览器供人工登录。
- `检查全部登录` 检查所有已添加店铺浏览器的登录状态。
- 批量创建草稿时，使用已添加的店铺浏览器；未添加店铺时不应启动批量任务。
- 浏览器生命周期绑定店铺，不绑定商品。不能每完成一个商品就关闭浏览器。
- 同一店铺内商品串行执行；商品保存草稿后，有下一个商品就直接开始新的发布流程。
- 一个店铺的全部商品完成后，浏览器回到商家后台首页并保持待命，直到用户点击关闭。
- 不同店铺可并发执行；人工验证、失败、日志、截图和状态必须按店铺隔离。
- 程序只允许点击保存草稿，不自动点击最终 `提交并上架`。
- 当前店铺列表是本次运行内存态；Profile 登录态会保留。后续可做店铺重命名、持久化账号列表和并发数控制。

## 开发计划

### 第 1 阶段：项目骨架和资料模型

目标：

- 建立 Python 项目结构。
- 定义商品草稿、SKU、图片资料、任务步骤和执行状态。
- 提供 CLI 校验入口。
- 写最小测试，确保模型校验逻辑可运行。

验收：

- 能加载一个 JSON 商品资料文件。
- 能输出校验错误。
- 核心模型测试通过。

### 第 2 阶段：浏览器自动化 Spike

目标：

- 使用 Playwright 启动或连接独立 Chrome/Chromium Profile。
- 手动登录拼多多商家后台。
- 程序检测是否已登录。
- 打通固定类目的最小流程：进入发布页、填写标题/价格/库存、上传主图、保存草稿。

验收：

- 至少 1 个商品能自动保存草稿。
- 失败时有截图和明确步骤。
- 不自动点击最终上架。

### 第 3 阶段：任务队列与恢复

目标：

- 用 SQLite 保存商品任务。
- 支持待处理、运行中、暂停、失败、完成等状态。
- 支持失败后从已知步骤继续。

验收：

- 批量导入 5 个商品。
- 中途中断后能继续未完成商品。
- 每个商品都有执行日志和最终状态。

### 第 4 阶段：OCR 辅助识别

目标：

- 建立 OCR 引擎接口。
- 接入 PaddleOCR。
- 用 OCR 识别异常弹窗、页面错误提示、截图区域文字。

验收：

- OCR 结果包含文本、置信度、截图来源。
- 自动化失败时能附加 OCR 识别出的页面提示。

### 第 5 阶段：桌面端

目标：

- 使用 PySide6 实现跨平台桌面壳。
- 支持导入资料、查看校验结果、启动/暂停任务、查看日志和截图。

验收：

- macOS 可运行。
- Windows 打包前完成路径和浏览器配置适配。

### 第 6 阶段：打包和平台验证

目标：

- macOS 和 Windows 分别打包。
- 验证 Chrome 路径、Profile 路径、OCR 模型路径、图片上传路径。

验收：

- 非开发环境可启动。
- 能跑通最小商品草稿流程。

## 当前实现约束

- 先实现小闭环，再扩展类目和字段。
- 新依赖必须服务当前阶段，不提前引入重依赖。
- 不写无法验证的浏览器脚本。
- 不做大而全配置系统，只有出现真实差异时再抽象。
- 不改无关文件，不做格式化 churn。
- 普通用户界面优先隐藏后台 URL、发布 URL、Profile、Artifacts、Channel、Timeout 等开发配置，除非进入专门的高级设置。

## 常用验证命令

项目早期优先使用：

```bash
python3 -m unittest
python3 -m compileall product_pilot tests
```

引入 pytest 后使用：

```bash
pytest
```

引入 Playwright 后补充：

```bash
python -m playwright install chromium
```
