[project]
name = "product-pilot"
version = "0.1.0"
description = "Local browser automation for preparing Pinduoduo product listings."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "openpyxl>=3.1",
]

[project.optional-dependencies]
automation = [
  "playwright",
]
desktop = [
  "PySide6",
]
ocr = [
  "paddleocr",
]
validation = [
  "pydantic",
]
dev = [
  "pytest",
]

[project.scripts]
product-pilot = "product_pilot.cli:main"
product-pilot-desktop = "product_pilot.desktop.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
